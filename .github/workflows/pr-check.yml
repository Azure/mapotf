name: build

on:
  push:
    branches:
      - main
  pull_request:

env:
  TOKEN: ${{secrets.GITHUB_TOKEN}}

permissions:
  contents: read

jobs:
  test:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c #v6.4.0
        with:
          go-version-file: go.mod
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e #v4.0.1
      - name: Run build
        run: |
          go build github.com/Azure/mapotf
      - name: Run tests

        run: go test -v github.com/Azure/mapotf/...
      - name: Test checksum signing helper
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable Pester | Where-Object Version -EQ '5.7.1')) {
            Install-Module Pester -RequiredVersion 5.7.1 -Scope CurrentUser -Force -SkipPublisherCheck
          }
          Import-Module Pester -RequiredVersion 5.7.1
          $tests = Join-Path -Path $env:GITHUB_WORKSPACE -ChildPath '.github' -AdditionalChildPath 'tests', 'Get-ReleaseChecksum.Tests.ps1'
          $result = Invoke-Pester -Path $tests -PassThru
          if ($result.FailedCount -ne 0) { exit 1 }
      - name: golangci-lint
        if: runner.os == 'Linux'
        run: |
          docker run --rm -v $(pwd):/app -w /app golangci/golangci-lint:v2.12.2-alpine golangci-lint run -v --timeout=3600s
      - name: Run Gosec Security Scanner
        uses: securego/gosec@9e6a9843d7a4a6e3e9a8539b02612c8a4aa3f889 #v2.27.1
        if: runner.os == 'Linux'
